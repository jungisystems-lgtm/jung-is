"""Django app configuration for landing copy."""

from django.apps import AppConfig


class LandingCopyConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "landing_copy"
